<div class="wrapper">
    <div class="sidebar" data-background-color="white" data-active-color="success">

<!-- TODO Add Sidebar Panel -->
     <%= render partial: "companies/sidebar" %>
    <div class="main-panel">
<!-- TODO Add Navbar Panel -->
    <%= render partial: "users/navbar" %>

   <!--   Big container   -->
    <div class="content">
      <!-- <div class="container"> -->
        <div class="container-fluid">
          <div class="row">
            <div class="col-sm-12">
                    <div class="card wizard-card box-shape" data-color="blue" id="wizard">
                        <%= form_for(@interview, url: {action: 'update'}) do |f| %>
                            <% if @interview.errors.any? %>
                              <div id="error_explanation">
                                <h2><%= pluralize(@interview.errors.count, "error") %> prohibited this interview from being saved:</h2>

                                <ul>
                                <% @interview.errors.full_messages.each do |message| %>
                                  <li><%= message %></li>
                                <% end %>
                                </ul>
                              </div>
                            <% end %>
                           <%= render 'form', f: f, d: 'Update Interview', n: 'Edit interview' %>
                          <% end %>
                    </div>
            </div>
          </div> <!-- row -->
      </div> <!--  big container -->
    <!-- </div> -->
  </div>

 </div>
</div>
</div>